import 'package:flutter/material.dart';

abstract final class AppColors {
  // Brand (teal-ish, not default M3 blue)
  static const brand = Color(0xFF16A394);
  static const brandDark = Color(0xFF0F6E66);

  // Surfaces
  static const neutralBg = Color(0xFFF6F7F9);
  static const neutralBgDark = Color(0xFF0F1214);
}

